<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel=icon href="assets/favicon.png">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,200&display=swap" rel="stylesheet">
    <script src="js/products.js" async></script>
    <link rel="stylesheet" href="css/product.css">
    <title>Products</title>
</head>
<body>
    <%- include('header'); -%>
    <section class="hero-section">
        <h1>Categor Name</h1>
    </section>
    <section class="product-items-section">
         <% for(let i = 0; i < products.length; i++) { %>
        <div class="product">
            <a href="#" data-product="<%= JSON.stringify(products[i]) %>" class="add-to-cart"><img src=<%- `assets/${products[i].image}` %> class="product-image" alt="product"></a>
            <a href="#" data-product="<%= JSON.stringify(products[i]) %>" class="add-to-cart"><h2><%- products[i].product_name %></h2></a>
            <a href="#" data-product="<%= JSON.stringify(products[i]) %>" class="add-to-cart"><p><%- products[i].price %></p></a>
        </div>
         <% } %>

    </section>
    <%- include('footer'); -%>
</body>
</html>